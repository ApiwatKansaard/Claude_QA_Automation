{
  "name": "widget-webhook-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-preset webhook server for EkoAI scheduled jobs — composes HTML from widget snippets and returns it to the callback endpoint.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "CALLBACK_DELAY_MS=500 node server.mjs"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "express": "^4.19.0"
  }
}
